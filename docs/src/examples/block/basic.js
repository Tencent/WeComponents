class Basic {
    constructor(config, items, container) {
        // 初始化页面是来属性与对象，与Vue链接
        this.pageConfig = config;
        this.items = items;
        this.container = container;
    }
}

export default Basic;
